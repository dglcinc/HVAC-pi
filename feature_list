look at how to convert the python scripts to a plugin
generify the python scripts for protocols (pin assignments, etc.)
try to get honeywell script to retrieve data using honeywell's APIs (probably means reverse-engineering headers)
create a means to monitor the pi and power cycle it if needed (may need to be a second pi or an iot-enabled switch...)
